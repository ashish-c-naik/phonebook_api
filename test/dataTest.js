var chai = require('chai')
var data = require("../data")
var expect = chai.expect;

describe('first', function() {
    it('first should return all the documents found', function() {
        // var pageSize = 1
        // return data.client_search("_hh55d5q42").then(function(result){
        //     expect().to.equal(1);
        // });
    });
});

